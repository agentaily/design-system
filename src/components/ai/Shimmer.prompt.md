Shimmering text — a moving highlight across the glyphs. For transient loading labels.

```jsx
<Shimmer>Searching the web…</Shimmer>
```

Pairs with `Reasoning`/`ToolCall` while a step runs. For block placeholders use `Skeleton`; for the response cursor use Message's `streaming`.
