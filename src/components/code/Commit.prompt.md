Git commit row with graph node and diff stat. Set `connected` for all but the last in a list.

```jsx
<Commit message="Fix focus ring" hash="7f29c4e" author="lin" additions={12} deletions={3} connected />
```
