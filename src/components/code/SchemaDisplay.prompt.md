JSON-schema / type tree.

```jsx
<SchemaDisplay schema={{
  model: { type: "string", required: true },
  messages: { type: "array", required: true },
  options: { type: "object", fields: { stream: "boolean" } },
}} />
```
