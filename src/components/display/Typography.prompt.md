`Prose` styles a block of raw HTML (headings/lists/code/links). `Text` applies a variant to a run.

```jsx
<Prose><h2>Heading</h2><p>Body with <code>code</code> and <a href="#">a link</a>.</p></Prose>
<Text variant="label">Section</Text>
```
