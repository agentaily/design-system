Hairline-bordered surface for grouped content; `ticks` adds the corner-tick motif.

```jsx
<Card eyebrow="Usage" title="This month" ticks>…</Card>
```

Padding: none / md / lg. Prefer borders over shadows — cards sit flat.
