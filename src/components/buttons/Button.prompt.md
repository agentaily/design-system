Agentaily's primary action button — mono-inverted fill, hairline secondary, quiet ghost.

```jsx
<Button variant="primary" size="md" onClick={save}>Save changes</Button>
<Button variant="secondary">Cancel</Button>
```

Variants: primary (inverted), secondary (hairline border), ghost, danger (outline red). Sizes sm/md/lg (28/36/44px). `full` stretches; `icon` takes a 16px svg.
