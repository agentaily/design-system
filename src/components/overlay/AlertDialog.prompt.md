Destructive-action confirmation modal. (For neutral content use `Dialog`.)

```jsx
<AlertDialog tone="danger" title="Delete conversation?" description="This cannot be undone." confirmLabel="Delete" onCancel={c} onConfirm={d} />
```
