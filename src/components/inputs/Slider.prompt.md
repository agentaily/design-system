Range slider with a square thumb and mono readout.

```jsx
<Slider label="Temperature" value={t} min={0} max={2} step={0.1} onChange={setT} />
<Slider label="Top-p" value={p} suffix="%" onChange={setP} />
```
