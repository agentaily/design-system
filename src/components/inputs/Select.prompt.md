Styled native select with drawn chevron.

```jsx
<Select label="Model" options={["agentaily-2", "agentaily-2-mini"]} value={model} onChange={e => setModel(e.target.value)} />
```

Options can be strings or { value, label }.
