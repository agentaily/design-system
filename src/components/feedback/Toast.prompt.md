Notification card with a thin status bar on the left edge.

```jsx
<Toast variant="ok" title="Saved" description="Conversation exported." onClose={dismiss} />
```

Presentational only — stack/position it yourself (bottom-right, gap 8px).
